# Contracts

This directory is reserved for shared API contracts between the dashboard and the orchestration layer.

Planned contents:

- generated OpenAPI clients for the front-end
- shared request and response examples
- strategy metadata schemas
- replay payload notes for chart rendering
